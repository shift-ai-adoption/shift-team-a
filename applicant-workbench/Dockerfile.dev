FROM node:22.17.0-bookworm-slim
WORKDIR /app
COPY package*.json ./
RUN npm ci && mkdir /app/data && chown -R node:node /app
COPY scripts/dev-container.mjs ./scripts/dev-container.mjs
ENV PORT=3200 HOST=127.0.0.1 DATA_DIR=/app/data
USER node
EXPOSE 3201
CMD ["node", "scripts/dev-container.mjs"]
