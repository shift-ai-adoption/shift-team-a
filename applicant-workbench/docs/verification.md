# 検証結果（2026-09-21）

## 字幕付き操作動画

- `video/penpot-beginner-ja.mp4`: H.264 / yuv420p / 25fps / 1600×1000、131.24秒、5,238,216バイト。
- Penpotの標準UIでネイティブ文字・図形を実際に編集。見出し変更、FILL色変更、取り消し／やり直し、保存後の再読込を録画。
- 再読込後、対象の文字が「申請者情報の登録」、対象のボタンの色が176b56であることを確認。
- 日本語キャプションを焼き込み。字幕19区間、MP4の章9区間（導入・本編6章・依頼例・おさらい）。音声なし。
- ブラウザ再生でduration=131.24、videoWidth=1600、videoHeight=1000、readyState=4、エラーなし。
- FFmpegで全映像デコードを実行し、エラーなし。抽出フレームを目視確認。
- Penpotの変更内容をCodexへ伝える部分は説明カード。アプリ本体のコード変更はこの動画作成では行っていない。

## 確認済み

- Compose構文検証成功。アプリ、Penpot frontend/backend/exporter、公式MCP、PostgreSQL、Valkey、MinIOを起動。
- 公開先は127.0.0.1限定。DB、Valkey、MinIOはコンテナ内部のみ。
- `npm run build` 成功。
- `npm test` 成功: 個人CRUD、入力チェック、法人番号重複、別利用者からの取得・更新・削除拒否、CSRFヘッダーとOrigin確認、楽観ロック、変更履歴。
- `npm run test:e2e` 3件成功: 個人と法人の新規登録→確認→完了→編集→再読込→削除キャンセル→削除、検索・絞り込み・空状態、利用者切替、390px幅でページ全体がはみ出さないこと。
- コンテナ再作成後のサンプルデータ保持。
- 6画面のPNGを取得。PC一覧、法人フォーム、スマートフォン一覧を目視確認。モバイル表は表内部で横スクロール。
- 8画面の編集可能SVGを `design/screens/` に生成。
- ローカルPenpotアカウントとデザインファイルを作成。
- MCPの `initialize` / `tools/list` 成功。公式プラグインのmanifest配信を確認。
- Playwright MCP 0.0.82のCLI引数を確認。

## 保留

- ユーザーの明示的な承認後、Penpot MCPプラグインへデザイン内容・ライブラリ・コメントの読み書き権限を付与し、接続を確認。
- MCP経由でテキストを「確認画面へ進む」から「進む」へ変更し、ボタンを文字幅に合わせて84pxへ縮小。読み取りと書き込みを検証済み。
- READMEの比較画像撮影のため、PenpotのPage 2は修正前の「確認画面へ進む」・幅260pxへ戻した状態で保持。
- ピクセル一致率は未評価。比較対象の承認済みベースライン画像がないため、99.9%等の数値は主張しない。
- 本番向け本人認証・代表権確認・アクセシビリティ認証は対象外。
